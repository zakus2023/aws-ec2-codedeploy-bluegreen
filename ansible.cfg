# Stops Ansible from using the 'script' plugin for inventory (avoids "Exec format error" on YAML in WSL).
# When the repo is on /mnt/c (world-writable), Ansible ignores ansible.cfg in this directory — set ANSIBLE_CONFIG to this file (see docs).
[inventory]
enable_plugins = amazon.aws.aws_ec2, auto
